{
  "name": "uss",
  "private": true,
  "packageManager": "pnpm@10.0.0",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "typecheck": "turbo typecheck",
    "test": "turbo test",
    "db:generate": "pnpm --filter @uss/db db:generate",
    "db:migrate": "pnpm --filter @uss/db db:migrate",
    "db:studio": "pnpm --filter @uss/db db:studio"
  },
  "devDependencies": {
    "turbo": "^2.1.3"
  }
}
